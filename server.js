const express = require('express');     //importing express using require | 'express' object lai instantiate gareko express constant ma
const app = express();                  //express() returns app

app.listen(5000);                       //runs on port 5000